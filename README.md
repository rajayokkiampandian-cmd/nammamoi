# assets/icons

`favicon.svg` is included. All other icons across the site (features, events, footer socials) are inline SVG directly in the HTML — add extra icon files here only if you want to replace them with custom artwork.
